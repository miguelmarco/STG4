import Game.Levels.FamUnion.L05unionunion  --It imports all previous levels.
/-!

# FamUnion world

-/
World "FamUnion"
Title "Mundo de uniones de familias"

Introduction
"
Si `F` es una familia de conjuntos de `U`, entonces la *unión* de `F` es el conjunto de los objetos
de `U` que pertenecen a al menos un elemento de `F`. En este mundo exploraremos las uniones de
familias.
"
